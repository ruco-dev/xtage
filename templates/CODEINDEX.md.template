---
repo_url: {{REPO_URL}}
last_indexed: {{DATE}}
tool_version: {{TOOL_VERSION}}
---

# Code Index: {{PROJECT_NAME}}

> Semantic index of {{PROJECT_NAME}}. Each section describes what a file or module *does behaviourally* — not what it contains syntactically. Generated by Claude from source; updated incrementally on each commit.

---

<!--
xtage generates one section per file or logical module below.
Format per section:

## `path/to/file.ts`

**Role:** One sentence on what this file does in the system — its job, not its contents.

**Key symbols:**
- `functionName(args) → ReturnType` — what it does, when callers should use it, side effects if any
- `ClassName` — what it represents, its lifecycle, usage contract

**Depends on:** other files/modules this file imports from (within repo only)
**Depended on by:** files that import from this one
**Last updated:** ISO8601

---
-->

---

*Generated by xtage {{TOOL_VERSION}} on {{DATE}}*
